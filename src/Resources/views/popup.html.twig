<div class="popup"{% if popup_id is defined %} id="{{ popup_id }}"{% endif %}>
    <button
        type="button"
        class="bt-popup-close"
        title="{{ 'Close pop-up window'|trans }}"
    >{{ 'Close pop-up window'|trans }}</button>
    <div class="content">{% if content is defined %}{{ content }}{% endif %}</div>
</div>
